#{{ "=" * 78 }}
# Generado por mks-idex-suite -- NO EDITAR A MANO (se sobrescribe al re-renderizar
# el perfil desde el asistente web). Placa: {{ profile.board.id }}
#
# ADVERTENCIA DE SEGURIDAD:
# Los pines de este archivo provienen de referencias de comunidad / Klipper
# mainline para esta familia de placa y PUEDEN NO COINCIDIR exactamente con tu
# revision fisica. Antes de la primera puesta en marcha (y en especial antes
# de la primera vez que calientes hotend/cama):
#   1. Verifica heater_pin y sensor_pin de [extruder] y [heater_bed] contra tu
#      placa fisica -- un pin de termistor incorrecto puede hacer que Klipper
#      "vea" una temperatura falsa mientras el heater real sigue encendido.
#   2. Confirma min_temp/max_temp razonables (no los deshabilites).
#   3. Con el hotend FRIO, ejecuta un primer test de PID/heater con supervision
#      directa y extintor/CO2 a mano, como con cualquier maquina que calienta.
#   4. Contrasta contra el archivo oficial equivalente en
#      https://github.com/Klipper3d/klipper/tree/master/config si existe.
#{{ "=" * 78 }}
